const settings = {
    backendUrl: 'http://localhost:8000',
    timerInterval: 5000,
    urls: {
        get_token: '/api-token-auth/',
        room_list: '/api/rooms/'
    }
}

export default settings
